@model PetLink.Models.User
@{
    ViewData["Title"] = "Forgot Password Form";
}

<div class="container mt-4 d-flex flex-column justify-content-center align-items-center">

    @* Intro com a marca *@
    <div class="text-center mb-3">
        <h1 class="display-6 text-primary-blue fw-bold">
            <i class="text-primary me-2">
                <img src="~/images/logo.png" alt="PetLink Logo" height="40"
                     class="d-inline-block align-text-top me-2">
            </i>PETLINK
        </h1>
        <p class="text-muted">Forgot my password.</p>
    </div>

    <div class="p-3">
        <div class="card shadow-sm" style="border: 2px solid var(--petlink-primary); border-radius: 50px; width: 400px;">
            <div class="card-body p-2 p-md-5">

                <h3 class="mb-2 text-primary-blue fw-bold">Password reset</h3>
                <p class="text-muted small mb-4">We sent a code to your email, @Model?.Email .</p>

                <form id="passwordForm" asp-controller="Profile" asp-action="PasswordResetForm" method="post" novalidate>

                    <div class="mb-3">
                        @* Estrutura com o OLHO *@
                        <div class="input-group mt-2">
                            <input type="number" name="code" class="form-control py-2" id="code" required>
                        </div>
                        <div class="invalid-feedback" id="codeError"></div>
                    </div>

                    <button type="submit" id="submitBtn" class="btn btn-primary w-30 py-2 mb-4 fw-bold rounded-pill">Confirm code</button>
                </form>
            </div>
        </div>
    </div>
</div>